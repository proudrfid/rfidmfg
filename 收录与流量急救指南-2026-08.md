# 收录与流量急救指南（2026-08）

## 补充：询盘中断诊断（2026-08-08 实测）

**结论：不是表单坏了，是流量在 7 月下旬断崖。** 证据链：① 实测提交 [TEST] 询盘 → Formspree 返回 200、邮件正常送达，表单→邮箱全链路正常；② Formspree 后台提交记录截止于 7 月下旬；③ WhatsApp 和直接邮件询盘同时停止——三个互不相关的渠道同时归零，唯一共同上游是网站访客本身。

**流量为什么断：新站蜜月期结束 + 相似内容去重。** 6 月中旬上线后 Google 快速收录并给了新站临时曝光（这就是 6–7 月询盘的来源），随后重新评估：a) Google 明确提示"省略了极为相似的条目"——25 个产品详情页全部被去重过滤，只留分类页；b) 站点与老站 mindrfid.com（成都 Mind IOT，100+ 语言、收录量大得多）产品、图片（去水印复用）、文案高度重合，Google 眼中老站才是"正主"；c) 6 月 24–26 日 Google spam update 恰在此窗口；d) 新域名零外链，蜜月期后没有任何权重支撑排名。查询 `site:rfidmfg.com` 现状：仅 ~20 个枢纽页在索引，首页排位极低，产品页（真正带询盘的页面）全军覆没。

**修复优先级（在原 A–E 基础上调整）：**

1. **GSC 验证（最急）**——"效果"报告能看到 6–7 月的展现/点击曲线和确切下跌日期，验证本诊断；同时检查"手动操作"里是否有镜像站/垃圾内容处罚。
2. **产品页差异化改造（治本）**——25 个产品详情页目前共用 ~80% 模板文案，且与 mindrfid.com 内容重合。需要：每页独有的产品描述/规格/FAQ；替换去水印复用的图片为独立拍摄图；明确两站关系（双品牌需彻底差异化，否则 Google 会持续把 rfidmfg 当副本过滤）。
3. **信任信号一致性**——rfidmfg 宣称深圳、mindrfid 实体在成都，两套公司身份 + 未经证实的"Top-3"及合作伙伴 logo，在 spam update 后属于高风险信号，建议统一为可验证的真实信息。
4. **外链五件套**——蜜月期结束后，排名恢复必须靠真实权重（见 E 部分）。
5. **GA4**——装上后流量波动第一时间在数据里看到，而不是等询盘消失才发现。


## 诊断结论（2026-08-08 核查）

网站技术上完全健康：Vercel 部署正常、全站 200、robots.txt 允许抓取、meta 均为 index,follow、canonical/sitemap 正确。零流量的原因不在站内，而是**上线后三件必做事一件都没做**：

1. **GA4 从未激活** — 全站统计代码仍被注释包裹、ID 是占位符 `G-XXXXXXXXXX`。有流量也测不到，统计永远是 0。
2. **Google 零收录** — `site:rfidmfg.com` 无任何结果；GSC 从未验证（页面无验证标签、DNS 无验证 TXT 记录）、sitemap 从未提交。
3. **新域名 + 零外链** — 上线不到 2 个月，链外建设清单仍是待办。收录后排名爬升也需要外链支撑。

---

## A. Google Search Console 验证 + 提交（10 分钟，最优先）

推荐 **DNS 验证**（域名 DNS 托管在 NameSilo/dnsowl，一次验证覆盖 www 和非 www）：

1. 打开 search.google.com/search-console → 添加资源 → 选左侧"**域名**"类型 → 填 `rfidmfg.com`。
2. 复制给出的 TXT 记录值（`google-site-verification=...`）。
3. 登录 NameSilo → Manage DNS（rfidmfg.com）→ 添加 TXT 记录：Host 留空，Value 粘贴上一步的值 → 保存。
4. 回 GSC 点"验证"（DNS 生效可能要等几分钟到 1 小时）。
5. 验证通过后：左侧"站点地图" → 分别提交 `sitemap.xml` 和 `image-sitemap.xml`。
6. 用顶部"网址检查"工具，对以下页面逐个点"**请求编入索引**"（每天有配额，先做最重要的）：
   - `https://www.rfidmfg.com/`
   - `/products/`、`/products/cards/`、`/products/cards/hotel-key-card/`
   - `/guides/rfid-frequencies-lf-hf-uhf/`、`/rfid-benchmark-2026/`

> 备选：如果不想动 DNS，选"网址前缀"类型 + "HTML 标签"验证法，把 meta 标签的 content 值发给 Claude，全站加上后重新部署即可验证。

## B. Bing Webmaster（5 分钟）

1. 打开 bing.com/webmasters → 登录 → 选择"**从 GSC 导入**"（做完 A 后一键完成，站点和 sitemap 自动同步）。
2. Bing 收录同时供给 ChatGPT 搜索、Copilot 等 AI 入口，对 GEO 价值高，别跳过。

## C. IndexNow（可选加速）

Bing/Yandex 即时推送协议。需在站点根目录放一个 key 文件——Claude 可以生成并加入构建，部署后一条请求即可批量推送全站 URL。做完 B 后如需要就说一声。

## D. GA4 激活（2 分钟创建 + Claude 全站接入）

1. 打开 analytics.google.com → 管理（左下齿轮）→ 创建媒体资源。
2. 媒体资源名 `RFID MFG`，时区/币种随意 → 平台选"网站" → 网址 `https://www.rfidmfg.com`。
3. 拿到 **衡量 ID（G- 开头）**，发给 Claude。
4. Claude 负责：替换全部 ~90 个源页面 + 4 个构建脚本中的占位符、取消注释激活、重建 dist、并加上表单提交 / WhatsApp 点击转化事件。

## E. 本周外链五件套（详见《链外与实体建设清单.md》）

性价比最高的起步组合：**Alibaba + Made-in-China + Global Sources 开店**，**Wikidata 建实体**，**Google Business Profile 认领**。每项资料统一口径：RFID MFG Co., Ltd. · https://www.rfidmfg.com · peter@rfidmfg.com · +86 755 2376 5843 · Shenzhen, China · Est. 1996。

## 预期时间线

提交后 Google 收录一般 3 天–2 周开始出现；竞争词排名爬升需 2–3 个月且依赖外链；B2B 平台（Alibaba 等）的直接询盘通常比自然搜索来得更快，建议两条线并行。GSC 验证 24–48 小时后即可在"效果"报告里看到真实展现/点击数据——以后判断流量看 GSC + GA4，不再是盲区。
