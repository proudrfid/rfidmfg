# 询盘接入与转化追踪 — 5 分钟设置指南

> 目标:让网站的询盘真正"落地"并可被衡量。三件事,按顺序做。
> 站内的表单优化(公司/电话/国家字段、免费样品勾选、反垃圾、信任标识)我已做好;只差下面这几步**需要你的账号**。

---

## A. 接通询盘表单(最重要,约 5 分钟)⭐⭐⭐

> 现状:`script.js` 里 `FORM_ENDPOINT` 还是空的,所以表单提交只会尝试打开访客邮箱——大多数人不会完成。**接通后,询盘会自动进你邮箱。**

**用 Formspree(最简单):**
1. 打开 https://formspree.io ,用 **peter@rfidmfg.com** 注册(免费版 50 封/月)。
2. 新建一个 Form,复制它给你的地址,形如 `https://formspree.io/f/abcdwxyz`。
3. 打开项目里的 `script.js`,找到这一行:
   ```js
   var FORM_ENDPOINT = '';
   ```
   改成:
   ```js
   var FORM_ENDPOINT = 'https://formspree.io/f/abcdwxyz';
   ```
4. 提交并部署:`git add -A && git commit -m "connect form" && git push`(Vercel 会自动重建)。
5. **第一次**有人提交后,Formspree 会给 peter@rfidmfg.com 发一封确认邮件,点确认即可开始收询盘。

> 想把询盘直接进飞书多维表?把 `FORM_ENDPOINT` 换成你的飞书 Worker 地址即可(代码已兼容)。需要我帮你写这个 Worker 时说一声(要先轮换 App Secret)。

---

## B. 装 GA4,看数据 + 量转化 ⭐⭐⭐

> 没有分析就是盲投。装上后你能看到:哪些页面带来询盘、访客从哪来、哪一步流失。

1. 打开 https://analytics.google.com → 创建 GA4 资源,拿到 **Measurement ID**,形如 `G-XXXXXXXXXX`。
2. **把这个 ID 发我,我一键全站激活**(每页 `<head>` 里已留好 GA4 代码块,注释着,替换 ID 后取消注释即可)。
3. 我已经在 `script.js` 里**埋好转化事件**,装上 GA4 后自动开始统计:
   - `generate_lead` — 提交询盘表单
   - `whatsapp_click` — 点 WhatsApp
   - `datasheet_download` — 下载产品规格书 PDF
   - `phone_click` — 点电话
4. 在 GA4 → 管理 → 事件,把 `generate_lead`、`whatsapp_click` 标记为 **关键事件(转化)**。

---

## C. Google Search Console + Bing Webmaster ⭐⭐

> 让搜索引擎更快收录,并看到真实搜索词。

1. 打开 https://search.google.com/search-console → 添加"网域"资源 `rfidmfg.com` → 按提示在 DNS 加一条 TXT 记录验证。
2. 验证后,提交站点地图:`https://www.rfidmfg.com/sitemap.xml`。
3. Bing:https://www.bing.com/webmasters 可直接从 Google Search Console 一键导入。

---

## D. 接通之后,这样跟进(直接影响成交)

- **设手机提醒**:把询盘邮件转发/推送到手机。B2B 黄金跟进时间是**收到后 1 小时内**联系,转化率最高。
- **样品请求优先**:表单里勾了"免费样品"的,优先、当天回。
- **WhatsApp 已是你目前最通的渠道**——保持随时在线。
- 每周看一次 GA4 + Search Console:哪页带询盘就加强,哪页有流量没转化就改 CTA。

---

## 我已经做好的(站内,无需你操作)
- 表单新增 公司 / 电话(WhatsApp)/ 国家 字段(更好跟进与筛选),必填仍只有姓名+邮箱(不增阻力)。
- "我想要免费样品"勾选框(样品意向 = 高质量询盘信号)。
- 蜜罐反垃圾字段(挡机器人);兼容 Formspree 的 `_gotcha`。
- 表单旁信任标识(1996 · ISO 三认证 · 免费样品 · 100+ 国家 · 2 年质保 · 24h 回复)+ "无义务、信息保密"微文案。
- 全站转化事件埋点(装上 GA4 即生效)。
