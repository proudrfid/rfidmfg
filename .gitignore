# Build output (Vercel rebuilds from source)
/dist/

# Deploy package
*.zip

# Original-image backups (dev only, large)
/images_orig_logo/

# Temporary scan/check images created during editing
_*.png

# Stray test files from image editing on the synced mount (delete on your Mac when convenient)
images/_wtest
images/rfid-animal-ear-tag-clean.webp

# Build artifact (regenerated by build-datasheets.sh)
products.json

# Dependencies / tooling / OS
node_modules/
.vercel/
.DS_Store
